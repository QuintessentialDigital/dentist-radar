import postmark from 'postmark';const token=process.env.POSTMARK_TOKEN;const from=process.env.POSTMARK_FROM||'no-reply@example.com';const sender=process.env.SENDER_NAME||'Dentist Radar';let pm=null;if(token) pm=new postmark.ServerClient(token);export async function sendEmail(to,subject,text){if(pm){return pm.sendEmail({From:`${sender} <${from}>`,To:to,Subject:subject,TextBody:text});}else{console.log('[EMAIL]',to,subject,text);return {mocked:true};}}